[package]
org = "wso2"
name = "log_crm_activity"
version = "0.1.0"
distribution = "2201.13.0"

# Resolved from the local Ballerina repository until published to Central.
[[dependency]]
org = "ballerinax"
name = "sap.businessone"
version = "1.0.0"
repository = "local"

[[dependency]]
org = "ballerinax"
name = "sap.businessone.crm"
version = "1.0.0"
repository = "local"
